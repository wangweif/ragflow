登录，更新代码
   ```bash
    ssh llm@192.168.8.250
    输入密码
    j ragflow
    git fetch;git rebase
    检查一下是否有本地的修改，如果有，使用git reset --hard
   ```


前端
   ```bash
    cd web
    nvm use 22
    npm install;npm run build
   ```

后端
   ```bash
   source .venv/bin/activate
   export PYTHONPATH=$(pwd)
   bash docker/launch_backend_service.sh
   ```